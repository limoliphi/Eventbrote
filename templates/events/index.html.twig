<h1>Events</h1>

<ul>
    {% for event in events %}
        <li>
            <strong>{{ event.name }}</strong>
            in{{ event.location }}
            ${{ event.price|number_format }}
        </li>
    {% endfor %}
</ul>
